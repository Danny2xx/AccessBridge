# Notebook-first dependencies.
# Heavier routing dependencies are added when Milestone 1 production code starts
# using R5/r5py.
#
# Use a stable CPython release, ideally Python 3.12. Python 3.15 alpha currently
# causes pandas/numpy to build from source on macOS because wheels are not
# available.
numpy>=2.0,<2.3
pandas>=2.2,<2.3
openpyxl>=3.1,<3.2
ipykernel>=6.29,<7
matplotlib>=3.8,<3.10
geopandas>=1.0,<1.1
pyogrio>=0.9,<0.12
pyproj>=3.6,<3.7
shapely>=2.0,<2.1
